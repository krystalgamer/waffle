/**
 * @defgroup notepad notepad module
 * Contains all the notepad code
 * @{
 */

/**
 * @brief Creates a terminus window
 * @return window id of terminus
 */
unsigned create_terminus();

/** @} */
